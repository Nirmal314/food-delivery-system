export const publicRoutes = ["/", "/restaurants"];

export const authRoutes = ["/login", "/signup", "/adminsignup", "/adminlogin"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
