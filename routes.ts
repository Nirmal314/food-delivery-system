export const publicRoutes = ["/", "/restaurants", "/searchfood"];

export const authRoutes = ["/login", "/signup", "/adminsignup"];

export const adminRoutes = ["/dashboard", "/menu", "/orders"];

export const userRoutes = ["/yourorders"];

export const apiAuthPrefix = "/api/auth";

export const USER_REDIRECT_ROUTE = "/";

export const ADMIN_DASHBOARD_ROUTE = "/dashboard";
